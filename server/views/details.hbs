
        <main>
            <section id="deatils-page">
                <div class="wrapper">
                    <div class="product-img">
                        <img src="{{homeImage}}">
                    </div>
                    <div class="product-info">
                        <div class="product-text">
                            <div class="product-text">
                                <h1>Name: {{name}}</h1>
                                <h2>Type: {{type}}</h2>
                                <h4>Year: {{year}}</h4>
                                <h4>City: {{city}}</h4>
                                <p>Description: {{description}}</p>
                              
                                {{#if tenants}}
                                 <p>People rented this housing:  {{#each tenants}} {{#if @index}}, {{/if}} {{tenant}}{{/each}}</p>
                                
                                {{else}}
                        
                                <p>People rented this housing: There are no tenants yet.</p>
                                 
                                 {{/if}}
                            </div>
                        </div>
                        
                        {{#if user}}
                        <!-- if there is no registered user, do not display buttons-->
                        <div class="product-btn">
                            {{#if isAuthor}}
                            <!-- Only for registered user and creator of the housing offer-->
                            <a href="/{{_id}}/edit" class="edit">Edit</a>
                            <a href="/{{_id}}/remove" class="remove">Delete</a>
                            {{else}}
                            {{#if hasSpace}}
                            {{#if isRenting}}
                            <p class="alRentHome">You have already rent this home</p>
                            {{else}}
                             <!-- logged in user with available pieces-->
                            <a href="/{{_id}}/rent" class="rentHome">Rent a home, available {{availablePieces}} housing</a>
                            {{/if}}
                            {{else}}
                            <!-- logged in user with no available pieces-->
                            <p class="no-housing">No Housing Available!</p>
                            {{/if}}
                            {{/if}}
                        </div>
                        {{/if}}
                    </div>
                </div>
            </section>
        </main>
